name: agnix

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate plugin & marketplace
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run agnix
        uses: agent-sh/agnix@77b84aceb75fa62e3258bb07a8e13b05906549af # v0.20.1
        with:
          target: claude-code
          config: .agnix.toml
          fail-on-error: 'true'
